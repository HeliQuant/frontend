import CampaignView from "@/components/garage/CampaignView";

/** /trade — YOUR floor: reads your connected local engine (getEngineUrl default). Register at /onboarding. */
export default function TradePage() {
  return <CampaignView mode="user" />;
}
