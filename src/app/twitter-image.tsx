/**
 * X / Twitter large-image card — reuses the same Night Garage card as the Open Graph image so the
 * preview is identical across X, WhatsApp, Telegram, Discord, etc.
 */
export { default, alt, size, contentType } from "./opengraph-image";
